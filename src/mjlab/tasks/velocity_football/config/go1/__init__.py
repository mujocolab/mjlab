"""Go1 football task registration.

Registration is intentionally deferred. The current football reference targets the
G1 humanoid, so the copied Go1 configuration must not register native velocity IDs.
"""

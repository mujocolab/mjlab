# 2026-08-27

## 今日目标

- 核对旧 DepthStudent 的真实教师 lineage。
- 冻结当前教师基线并更新项目管理入口。
- 排除大型训练产物后提交并推送当前工作区。

## 已完成

- 确认教师为 2026-08-14 B1/A1R0 LongDropout10 `model_49999.pt`。
- 计算 checkpoint SHA-256：
  `b9eff9ad3bbcc043393dd3f1c259a013878f4e85aed2667fb5633290641c5930`。
- 将基线 ID 冻结为 `DT-TEACHER-BASE-0`；确认冻结 MLP DepthStudent 基线和
  constrained DepthStudent 候选。
- 核对18:42全量解冻 student 与23:28 constrained student 的末步训练指标。
- Git 忽略规则增加 checkpoint、ONNX和事件文件；`logs/`、`.venv/`继续排除。
- 活动代码收敛为4个 Task ID和3个训练脚本；移除Klavier任务、复制资产和冗余脚本。
- 新增根目录 `BASELINES.md`，记录三个外部 checkpoint 及 SHA-256。

## 验证

- 活动注册表检查：足球 Task ID 恰好为4个。
- 足球、DepthStudent 与 sim2sim 测试：126通过；仅有既有 ONNX 导出弃用/常量折叠警告。
- Ruff 静态检查通过；3个保留脚本通过 `bash -n`。

## 下一步

- 完成测试、提交并推送当前分支。
- 建立教师与两个DepthStudent共用的固定sim2sim评估manifest。

# 接手指针

> 这是中断后恢复工作的第一读取点。只保留当前状态，历史过程写入每日或实验记录。

## 当前上下文

- 最后更新：2026-08-27
- 负责人：项目负责人（待补充姓名）
- 当前分支：`recovery/temporal-history-20260803`
- 当前 run 基准 commit：`3344222`，教师 run 另有 `git/mjlab_soccer.diff`
- 当前里程碑：M3 定向运球 + M5 实机准备
- 关联决策：足球推进与可控区域奖励、机器人正则奖励
- 关联实验：`DT-TEACHER-BASE-0` B1/A1R0 LongDropout10 50k 教师

## 已完成

- 完成 8/3 计划的代码拆分与续训 smoke test；task ID、历史长度、num_envs=4096
  和 ONNX 形状校验已进入启动流程（缓解 R-007）。
- `dropout10` 分支完成 0→20k→50k 全程训练并导出 ONNX
  （`2026-08-06_22-56-57_..._resume20k_to50k_wandb/model_49999.pt`）。
- `episode_loss5_no_ball_term` 完成 0→5k→15k 续训（该 run 在 15k 处被用作分支点）。
- 速度包络惩罚 `command_velocity_envelope` 权重对比：`w=-4` 在 86 iteration 内
  回报塌到 -25、`ball_out_of_control` 终止率飙到 3.9，已终止；`w=-1` 稳定，
  完成 15k→20k 并导出 ONNX。
- `episode_loss5_envelope30_w1` 已于 2026-08-09 完成 20k→50k，保存
  `model_49999.pt`；末步 mean reward 19.65、episode length 954.35。
- 冻结 `FB-BASE-0` 的 `model_49999.pt` 为下一阶段配置和 checkpoint 基线。
- 复核 2026-08-21 DepthStudent lineage，确认其教师为 2026-08-14 B1/A1R0
  LongDropout10 `model_49999.pt`；现将该教师升级为当前唯一基线。
- 核对教师 checkpoint SHA-256 为
  `b9eff9ad3bbcc043393dd3f1c259a013878f4e85aed2667fb5633290641c5930`。
- 核对最近 DepthStudent：18:42 全量解冻 mixed rollout 末步 reward `30.04`、episode
  length `875.84`；23:28 constrained-latent 候选末步为 `71.47`、`970.19`。

## 当前状态

- 当前没有训练进程。
- 当前唯一基线：`DT-TEACHER-BASE-0/model_49999.pt`。本机外部产物位于
  `/home/ut/football_project/log_old/logs/rsl_rl/g1_velocity_football/`
  `2026-08-14_11-44-01_B1_A1R0_longdropout10_isaac_actor_dr_flat_`
  `seed42_from_walk16000_to50k_wandb/`。
- 教师 iteration 49999：mean reward `33.55`、episode length `956.23`。
- `FB-BASE-0`、WALK-0、Klavier SchemeA 均保留为历史或候选，不承担当前基线身份。

## 唯一下一步

使用 `DT-TEACHER-BASE-0/model_49999.pt` 建立固定评估结果，保存 manifest、逐 episode
指标和视频；随后用同一 manifest 比较 18:42 与 23:28 DepthStudent。

## 恢复工作步骤

1. 读取 [项目状态](项目状态.md) 和本文件。
2. 运行 `git status --short --branch` 和 `git log -5 --oneline`。
3. 确认没有遗留训练进程，并检查目标实验登记的 baseline ID。
4. 核对外部产物路径和 run ID 是否仍可访问。
5. 执行「唯一下一步」，不同时开启新的未记录工作。

## 阻塞与注意事项

- 新发现：`CurriculumManager` 没有 `state_dict`，续训时命令范围课程从基础范围重置。
  父 run 已经到 `lin_vel_x_max=2.0`，续训后回到 1.0，需要重新爬升。跨 run 比较
  课程等级或速度误差时必须考虑这一点。
- `error_vel_yaw` 在 20k 仍约 2.15，速度包络惩罚（w=-1）没有明显改善角速度跟踪。
- `episode_loss5_envelope30_w1@50k` 末步 `error_vel_yaw` 约 1.90，仍需固定评估确认。
- 新实验必须登记 `baseline_id=DT-TEACHER-BASE-0` 和 checkpoint SHA-256；不得把
  Klavier SchemeA 或某个 DepthStudent 候选静默替换成教师基线。
- 阻塞：仍未取得球—body 接触日志，不能可靠指定膝部/非足部惩罚对象（R-006/Q-005）。
- 当前工作区准备整体提交；旧实验的准确代码状态仍以对应 run 的
  `git/mjlab_soccer.diff` 为准。
- 大型 checkpoint 和 TensorBoard 事件保存在 run 目录，不纳入 Git。

# 阶段总结（2026-08-27）

## 当前结论

当前唯一基线调整为旧 DepthStudent 实际使用的坐标教师 `DT-TEACHER-BASE-0`。此前
`FB-BASE-0` 保留为历史配置基线。活动代码现只保留坐标 Teacher、冻结 MLP
DepthStudent 基线、constrained DepthStudent 候选和一个环境 smoke task；完整清单见
[BASELINES.md](../BASELINES.md)。

## 基线身份

| 字段 | 冻结值 |
|---|---|
| Run | `2026-08-14_11-44-01_B1_A1R0_longdropout10_isaac_actor_dr_flat_seed42_from_walk16000_to50k_wandb` |
| Checkpoint | `model_49999.pt` |
| SHA-256 | `b9eff9ad3bbcc043393dd3f1c259a013878f4e85aed2667fb5633290641c5930` |
| 代码状态 | commit `3344222125d6becde4968b3a9cf8accb2cffeb5d` + run 内 diff |
| 机器人 | 标准 MJLab G1 |
| Actor | 5帧本体 490维 + 10帧×7维足球历史，MLP `512/256/128` |
| BallCNN | 通道 `64/64/64`，kernel 3，dilation `1/2/4`，causal last-output |
| 训练结果 | iteration 49999：reward `33.55`、episode length `956.23` |

本机外部产物位于 `/home/ut/football_project/log_old/logs/rsl_rl/g1_velocity_football/`
对应 run 目录。checkpoint、事件文件和 ONNX 不纳入 Git。

## 观测与奖励边界

- 足球历史包含球位置 2维、双脚到球向量4维和可见掩码1维，共10帧。
- 球位置和双脚向量各自使用0--2步随机延迟，可见掩码为当前帧；旧教师不保证7维
  特征来自完全相同的控制帧。
- 使用 LongDropout10：约10%的非站立 episode 在2--6秒后失明至结束。
- 使用命令速度包络惩罚 `-1.0` 和 action acceleration 惩罚 `-0.1`。
- 教师训练使用坐标观测；相机、深度和YOLO链路的性能必须单独评估，不能由训练回报
  推断。

## 候选策略

- 2026-08-21 18:42 全量解冻 DepthStudent：step 9999 reward `30.04`、episode length
  `875.84`。
- 2026-08-21 23:28 constrained-latent/last-MLP 候选：step 9999 reward `71.47`、
  episode length `970.19`。训练指标更好，但尚无统一固定评估，因此不能升级为基线。
- 2026-08-26 Klavier SchemeA 属于不同策略族，已连同复制资产和任务注册从活动代码
  移除；训练事实仅作为历史记录保留。

## 下一步

1. 冻结同一 sim2sim manifest、seed、命令序列和初始球位。
2. 先用教师的足球真值位置评估控制上限，再评估视觉 observer 引入的性能损失。
3. 在同一 manifest 下比较教师、18:42 student 和23:28 student。
4. 保存逐 episode 指标和视频；完成前不按训练 reward 重新指定基线。
